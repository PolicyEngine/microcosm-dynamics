# A12-T2-R04/R05/R06 generation-2 certification ceremony report

## Status

**STATUS: LAWFUL-STOP** — Stage 2 proved that a retained, source-classified 1971
purpose prompt has no admitted exact source-token reference under either
§19.3.3 join branch. A12-T2-R04 is conjunctive and fail-closed, so neither
independent implementation can lawfully emit the complete prospective source
member. Sections 26.11.2 and 29.4.1 consequently prohibit instantiating the
builder, validator, R05 certificate, or R06 result.

No Q5 input, authority, full G17-C01 row, production output, or other
forbidden downstream object has been emitted.

## Ordered plan and disposition

| Stage | Required operation | Disposition |
|---|---|---|
| 0 | Reauthenticate revision-20 operativity, read the controlling law and precedent, fingerprint the checkout, and resolve the former revision-binding gap. | Complete. |
| 1 | Reconstruct the exact 279-row build-input domain and reauthenticate all six source comparands. | Complete. |
| 2 | Implement the independent `R04X-7F2A` builder. | Lawful stop before first-add: full-source reconstruction found an unjoinable mandatory purpose prompt. |
| 3 | Implement the independent `R04X-C91D` validator and its static/runtime independence audits. | Not reached; noninstantiation required. |
| 4 | Build, commit, and publicly validate the fixed-path R04/R05 certificate. | Not reached; noninstantiation required. |
| 5 | Execute every inherited and Amendment-18 mutation attack. | Not reached. |
| 6 | Reproduce the Amendment-11 expected abort in the enacted sandbox and first-add the durable R06 result. | Not reached; R05 did not pass. |
| 7 | Run pinned and full verification, formatting and hygiene; finalize this report. | Not reached beyond stop hygiene. |

Each reached stage has one topology-preserving commit. No fixed R06 result was
first-added because no committed R05 certificate passed from a
strict-ancestor commit.

## Stage 0 — reconnaissance and revision binding

### Checkout fingerprint

- Branch: `sol/a12-tier2-r04-r05-r2`.
- Starting `HEAD`: `ae68be83dc564e8ce49d725fffa8621c857f5446`.
- Starting tree: `420f1e1c295d0bdd0da00b88f666f355d427379e`.
- Sole parent: `0262efacf88e86771e31910102d083824354bc2e`.
- `HEAD` and `origin/master` selected the same revision-20 commit at
  reconnaissance time. The local `master` branch label was stale and was not
  used as an authority.
- The only pre-existing untracked ceremony-support paths were
  `.ceremony-log/` and `CEREMONY_PROMPT.txt`; both are preserved and excluded
  from governed commits.
- Revision-20 design: mode `100644`, 3,964,278 bytes, Git blob
  `016c0fff757b54da730ae0044216416cde2d2c33`, raw SHA-256
  `631d3b2b8ecab1c29ec0595550a6d2b798f49ff96e74c722801d24c48ab111ec`.
- Amendment-18 closure: mode `100644`, 842 bytes, Git blob
  `19855c8d3f2cf2322d35fa67a330e96fc1afa913`, raw SHA-256
  `0080de3cc529d2f732835316a5566e58c887a9bd7592259acfe35ecaa3813fca`.

### Controlling law and precedent

The controlling text was read in the required order: §32 in full; §29.4 and
§29.5.2 as composed by §§30–32; §26.11.2; and §27.2.3. The generation-1
Stage-0 report, the ordered R03 archive/master witness, and the revision-20
registry state were also inspected. Design bytes, `gates.yaml`, `runs/`, and
all committed ceremony artifacts remain immutable.

The source-member boundary is the prospective G17-C01
`hierarchy_annotation_authority` subpayload only. Certification remains
pre-Q5 source-only NONAUTHORITY. R06 must reproduce the independent
Amendment-11 abort and terminate at `A19_SUCCESSOR_PROGRAM_STOP`.

### Public-oracle result

The unmodified public `validate_ratification_operativity()` was called from a
clean revision-20 checkout. It completed full pin, Git, artifact, and design
verification and returned the ordered closure domain:

```text
revision=20
ordered_domain=(13, 14, 15, 16, 17, 18)
closure_count=6
selected_position=2
selected_amendment_number=15
```

This exactly satisfies §32.3: `20 >= 18`, and the domain deep-equals
`tuple(range(13, 20 - 1))`. Only after the complete current snapshot passed
was the historical Amendment-15 closure selected at zero-based position 2.

### Revision-binding memo

The generation-1 stop is conclusively resolved. Section 32.3 expressly
supersedes the revision-17/revision-18-literal R05 locator and requires the
complete current terminal snapshot at every revision `R >= 18`, followed by
selection of Amendment 15 at proved position 2. It forbids a historical
loader, caller-supplied or cached snapshot, closest-revision fallback,
partial A15-only validation, and a fixed current maximum.

The serialized 11-key historical Amendment-15 binding is unchanged; the
enclosing current revision is deliberately not serialized. Revision 18
remains the required historical first-operativity ancestor, while the
certificate first-add must also descend from the current operative
revision-20 state. There is therefore one mandatory execution and no residual
composition choice.

### Environment and baseline reference

- Required setup was attempted with `uv venv --allow-existing` and
  `uv pip install -e ".[dev]"`.
- The sandbox forbade the default `~/.cache/uv` write, so `UV_CACHE_DIR` was
  relocated to `/private/tmp`. Package resolution was then blocked by the
  sandbox's intentional DNS/network denial.
- The worktree was linked to the repository's shared ratification environment
  and all Python/test commands use `uv run --no-sync`; Python is 3.14.4 and
  pytest is 9.1.1. Repository-global `black` and `ruff` executables are
  available for Stage 7.
- The controlling repin baseline reference is 5,571 passed, 91 skipped, and
  the same two known historical failures. Stage 7 will compare a fresh full
  run against that fingerprint rather than treating either historical failure
  as a ceremony regression.

## Stage 1 — build-input domain

The complete §32.2 envelope was constructed ephemerally and was not written
to the repository. The reconstruction authenticated the two Git-root
artifacts, all four registered questionnaire-capture inputs, both complete
upstream disposition relations, every included source file under
`/Users/maxghenis/PolicyEngine/psid-data`, and every one of the 22
repair/seal/evidence paths against both `HEAD` and working bytes.

The source projection had exactly these role counts:

| Role | Rows | Authenticated bytes |
|---|---:|---:|
| `questionnaire_flow` | 81 | 1,226,736,045 |
| `dictionary_layout` | 86 | included below |
| `codebook` | 47 | included below |
| `raw_fixed_width_data` | 43 | included below |
| Three field-source roles combined | 176 | 1,514,409,083 |
| Complete source domain | 257 | 2,741,145,128 |

The questionnaire link dispositions reproduced `81 / 1 / 383` for included,
out-of-wave 2025, and other. The accepted-document dispositions reproduced
`81 / 1 / 374`. All 81 selected documents and all 176 field-source files
were regular, nonsymlink files whose complete bytes matched their registered
sizes and SHA-256 values.

The six enacted comparands were freshly recomputed, not copied:

| Comparand | Derived value |
|---|---|
| `questionnaire_document_count` | `81` |
| `questionnaire_document_keyset_sha256` | `3326c9ba70b7f83f19b0ea934630d26ced73f230be1628cb74031d17160cb1a5` |
| `questionnaire_document_domain_sha256` | `b06139b147391d06b4f90a8f28de472a936ec08b3e9eb37001a5a70e2b3c3543` |
| `source_document_count` | `257` |
| `source_document_keyset_sha256` | `8b7cad855b791c5cd7d235a74d4a0f1ecc7511dc0458db11d6b04c1b6af2c36a` |
| `source_document_domain_sha256` | `9d7a98db7c2889eba150f70935f492aebbc41cd521e4139dc1ec886ecd9945ce` |

The 22 repair/seal/evidence identities totaled 4,345,235 bytes. Their paths
were sorted once by unsigned UTF-8 and reproduced path-array SHA-256
`504159116708ee4d5e2cc8abec130ca8679d22cce928dca42af12be305361c17`.
The final class boundary was exactly 257 `source_document` rows followed by
22 `repair_seal_evidence` rows.

Python sorted-key, compact, ASCII, no-NaN serialization with one terminal LF
produced exactly 168,504 envelope bytes and the newly derived digest:

```text
tier2_build_input_domain_sha256=
f34ced6e80e1bf72e68635b4f729c5b983c094fd25d16105a6c161ccd52fff63
```

The envelope itself remains ephemeral as §32.2.3 requires.

## Stage 2 — `R04X-7F2A` builder

### Adversarial reconstruction result

Two isolated reconstruction lanes began from the authenticated Stage-1
envelope, consulted neither a candidate certificate nor a shared
reconstruction implementation, and independently enumerated the source-side
positive-field joins. Both stopped before creating a governed implementation
path. One lane found the byte-ineligible `Family` prompt below; the other
found the independent `Dl7.`/`D17.` mismatch described afterward.

An earlier diagnostic observation that historical purpose-classification
rows were sparse was expressly rejected as a stopping theory. Section
26.11.2 requires fresh full-source semantic reconstruction, so sparsity in a
predecessor convenience relation is not itself a law gap. The stop instead
rests on authenticated prompt and field bytes for a retained, complete
classification row and the exact mechanical join rules.

### Primary retained prompt witness

The source boundary is the pinned source commit/tree
`19fa24c161e800e004320f0c10e81bce8831af68` /
`e35f9cd65017ece46de2f0c0dbc57f4321c0b8d4`. Its document-007 annotation has
these independently authenticated identities:

| Member | Exact value |
|---|---|
| Path | `docs/analysis/rq_stage2_annotations/document_007_fam1971_QxQs_annotation_v1.json` |
| Mode / Git blob | `100644` / `241a7b5d2ec9085d092bd4ea6387c7cd9760542b` |
| Bytes / raw SHA-256 | `3,142,928` / `f91c12e095c24c1c02fd2284ae8ffd809220868788d2dae2220e51c0e1f524d4` |
| Artifact ID | `rq-stage2-document-annotation:ba88d97c3fcf7cf06618582ddf174b107161f8c5b50355fdc5c8ee9ab1896eaa` |
| Embedded content SHA-256 | `4eb4525de946ce1d22e39334b9dd7a3d0c55dfe01e14eb4a4f4665167a1d0423` |

The complete 200-row occurrence domain contains, at position 149,
`psid-questionnaire-occurrence:c1bd2200374a04dc2cdd70bfdce80b07cfb00ac2372d5f498e03aa59a890d024`.
It is a wave-1971 `field_purpose_prompt` on page 49 at UTF-8 bytes
`[96,304)`, under the root flow, with matched-text SHA-256
`3190dfe60cf692c6bd25fa46664e463b8ae956583a2dcb933252da31fafbb512`.
Its exact text is:

```text
Family income is, of coun:e, this study's single most important
      measure of economic status. Thus it is important that you try to
      get complete and accurate responses. If the respondent is reluctant
```

The complete 99-row local-purpose domain classifies it at position 73 as
`interview_and_role_attachment`, with ID
`rq-local-field-purpose:c1fc958f1a5d3b128f1668ce06782243f838065e49021390870f616741ace3ce`
and canonical-row SHA-256
`b9e3460ec594bed6e23dfffa09b440a3fc87d2a6b2e3d2cf110aa4f15987fc46`.

The raw questionnaire source
`documentation/capture1/fam1971_QxQs.pdf` was reauthenticated at 24,196,248
bytes and SHA-256
`bf11433773143970a0d63311359bea435e66e4354c2bd2d61d28225411b9ef54`.
Pinned Poppler 26.04.0 reproduced 92 pages. Page 49 was exactly 2,392 UTF-8
bytes with SHA-256
`3cd65815027febe23dd208279f46bfe765ec6e373305048ef79fa00123bd3abd`;
slicing `[96,304)` reproduced the text and occurrence digest above.

The current document-007 repair overlay is append-only: it records
`predecessor_source_rows_retained: true` and
`predecessor_source_row_erasure_permitted: false`. Its only two named
supersessions are `/local_repeat_or_alias_evidence_rows/7` and
`/local_repeat_or_alias_evidence_rows/14`. An exhaustive scan of all 14
overlays found no occurrence of this classification ID or its pointer.
Therefore no enacted successor erases, replaces, or semantically redirects
the prompt.

### Complete same-wave field denominator and empty join

The four registered 1971 physical inputs were freshly authenticated:

| Role | Path | Bytes | SHA-256 |
|---|---|---:|---|
| Stata dictionary/layout | `family/1971/FAM1971.do` | 62,523 | `2d86429c3021eac15e99faa8b7b6bafc10194f37933d5f48b96d35aa234efcee` |
| SPSS dictionary/layout | `family/1971/FAM1971.sps` | 56,357 | `8a6ccfc9ba6110dabe8147b528c2e0b2ada2d701889ea9bb03aa6c9bf0d479ae` |
| Raw fixed-width data | `family/1971/FAM1971.txt` | 4,723,840 | `97360718a1f76e910ff2f2941dc0c65d6022016a570d53435b8ede97cef0b73c` |
| Codebook | `family/1971/FAM1971_codebook.pdf` | 1,025,137 | `59b10570210bdbd1cb9b07e09d946fd4942b477f49e565d9bea169e4fcc87bac` |

The `.do`, `.sps`, and codebook each independently yielded 555 ordered
canonical field rows. Their raw-field-ID streams deep-equal, so the
stable-first `D_1971` contains exactly 555 unique IDs; its audit-only
canonical-array SHA-256 is
`4df10943f57768169c563280312f58996705302b1bd1cebdc47361c008e00ca9`.
All 555 IDs contain an ASCII digit.

Exhaustive application of the two enacted branches to the primary prompt is
terminal:

| §19.3.3 branch | Derived result |
|---|---|
| `exact_raw_field_id_token` | Zero candidates. None of the complete 555-member `D_1971` occurs in the prompt; the prompt contains no ASCII digit. |
| `exact_question_identifier_token` | Zero candidates. Its complete leading printable nonspace ASCII token is `Family`, which has letters but no digit and therefore fails the mandatory letter-plus-digit predicate before any field-member comparison. |

The second lane independently found another retained positive on page 19:
`psid-questionnaire-occurrence:01d1913112b4b951d4422662c30ed47da3a4d93d3fc9a32da4bde64d16226d39`,
classified as `hours_worked`. Its exact leading token is `Dl7.` with lowercase
`l`; the only semantic near-match is codebook field `V1999`, whose description
begins byte-distinct `D17.` with digit `1`. Exhaustive enumeration over all
1,665 canonical field rows returned zero candidates. The other prompt on the
same QxQ context begins ineligible `017,`, while the correctly printed `D17.`
in core `q71` belongs to a different singleton component class and H
coordinate with no alias support. Thus no same-positive prompt or separate
questionnaire coordinate can rescue the join.

### Controlling failure and noninstantiation

Section 19.3.3 requires every annotated `field_purpose_prompt` to be consumed
by at least one `O_P` tuple, enumerates both exact branches for every positive
prompt, requires every positive join's reference array and raw-field domain
to be nonempty, and says that a positive without an admitted exact
source-token reference remains `registration_required`; no semantic guess
may fill it. Section 19.4.1 restates that each purpose prompt joins to a
unique same-wave field. The primary witness has zero lawful reference rows,
so no `raw_field_id`, field-source row, nonempty join, or complete
`hierarchy_annotation_authority` value exists under the immutable law.

Section 26.11.2 makes purpose expansion and field joins a conjunctive R04
gate and requires any missing witness or unresolved item to abort without a
partial catalog. Section 29.4.1 forbids stubbing or first-adding the future
implementation and certificate paths before the full source build and both
complete reconstructions exist. Consequently Stage 2 stopped before the
builder's first-add. Creating either program with a partial member,
normalizing `Dl7.` to `D17.`, attaching prose by semantic similarity, or
omitting either prompt would be a workaround and therefore a ceremony
failure.

## Stage 3 — `R04X-C91D` validator

Not reached. The fixed validator path remains absent as §29.4.1 requires.

## Stage 4 — committed R04/R05 certification

Not reached. The fixed certification path remains absent; R05 is not claimed.

## Stage 5 — mutation census

Not reached. No certification mutation result is claimed.

## Stage 6 — durable R06 expected-abort result

Not reached. Section 32.4.4 permits the R06 result's first-add only after a
committed, passing R05 certificate. The certificate does not exist, so the
fixed R06 result path remains absent and the Amendment-11 process was not
misrepresented as an R06 execution.

## Stage 7 — final verification and hygiene

Only lawful-stop hygiene was performed. The unmodified public operativity
oracle still returned `(13, 14, 15, 16, 17, 18)`. The builder, validator,
certificate, and R06 result fixed paths were all confirmed absent. Design
bytes, `gates.yaml`, `runs/`, committed ceremony artifacts, and the two
pre-existing untracked ceremony-support paths remain untouched.

## Exact resumption condition and commands

There is no lawful command that resumes Stage 2 under revision-20 law and
the frozen source bytes. A successor amendment must expressly resolve the
unjoinable retained purpose prompts, be ratified, and become operative through
a later registry repin. Only then may execution restart at Stage 0 in a fresh
worktree. The first commands after that external state change are:

```sh
cd /Users/maxghenis/PolicyEngine/social-security-model
git fetch origin --prune
git worktree add -b sol/a12-tier2-r04-r3 \
  /Users/maxghenis/PolicyEngine/social-security-model-worktrees/a12-t2-r04-r3 \
  origin/master
cd /Users/maxghenis/PolicyEngine/social-security-model-worktrees/a12-t2-r04-r3
uv venv --allow-existing
uv pip install -e ".[dev]"
```

The new run must read its then-controlling law and invoke the unmodified
public oracle before any source build. This report does not predict the
successor revision or closure domain.
