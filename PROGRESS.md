# Unit 2b Referee Fix Progress

## State

- Branch `claude/ce-codebook-inventory` starts clean at `bc39940`.
- The three requested referee edits are pending.

## Done

- Confirmed the requested branch and starting commit.

## Next

1. Bind the `V-B8 new_role_background ×10` checkpoint bytes that establish
   the new/split-off/recontact universe.
2. Remove or demote the six `family_archive_capture_record` residuals.
3. Split `V-B6 annual_job_match` into its questionnaire-absence residual and
   its non-blocking temporal-attachment production branch.
4. Rebuild and repin the adjudication artifact, update residual counts, format,
   and run the relevant suites.
