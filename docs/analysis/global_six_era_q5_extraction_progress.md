# Global six-era Q5 extraction progress

## State

- Branch: `claude/ce-global-q5-extraction`
- Scope: the one global Class-A questionnaire-slot closure authorized by
  section 19.3.3: the 81-document relationship catalog, six era annotations,
  43-wave hierarchy, and exhaustive absence domain. Class-B grammar rows are
  out of scope.
- Status: stopped at the lawful non-authority boundary for Q5, with the
  reproducible global source/catalog prerequisite and the first three era
  evidence slices complete. Three era slices remain. No Q5 or downstream slot
  artifact has been emitted.

## Done

- Verified `covered_earnings_correction_registry.design_binding()` returns
  revision 7, ratification commit `985be84fdeec70ffd20aa1e60dec7d300b7a555b`,
  and design SHA-256
  `8f90dd1aee59e6857418d2a73b617e5cb3991eba3a237a78303586a8c2a9debc`.
- Confirmed the worktree starts at `3c44529e8cff20df156d2d897fa384c6c85487db`
  on `claude/ce-global-q5-extraction` with no pre-existing changes.
- Located and began reconciling both fail-closed era audits, the registered
  questionnaire corpus, the codebook inventory adjudication, and the
  section 19.3.3 source-only hierarchy law.
- Confirmed the external final-report target is
  `/Users/maxghenis/m6-sol-lanes/sol-ce-globalq5.out`; no root `PROGRESS.md` or
  `FINAL_REPORT.md` is being created.
- Reauthenticated both pinned roots, all four questionnaire capture inputs,
  all 465 link and 456 accepted-document dispositions, and all 257 included
  staged files. The reconstructed `U` domain has the exact 81 questionnaire,
  86 dictionary/layout, 47 codebook, and 43 raw-file roles and reproduces the
  section 19 keyset/domain digests.
- Reproduced all 10,190 questionnaire pages with pinned Poppler 26.04.0 and
  committed their ordered page IDs, corpus digest-row locators, UTF-8 byte
  counts, and page-text SHA-256 values in the global prerequisite evidence.
- Constructed the three mandatory baseline sentinel relationships with exact
  section 19 ID preimages. The complete `R_Q` count and rows remain explicitly
  null: an honest global catalog requires the still-absent complete occurrence,
  flow, anchor, source-component, and alias annotation.
- Confirmed Q5 cannot be emitted in this Class-A-only lane because its
  `source_document_manifest.field_source_derivation` must carry the complete
  section 19.3.2 numeric-grammar relation. The separate 2023 `G13.` to
  `ER83121`/`ER83495` ambiguity also aborts the positive-field join.
- Built the 1968--1975 prerequisite slice from 16 registered documents and
  842 pages. Its 48 design-fixed baseline coordinates (1,680 after the
  35-purpose expansion) are an explicit lower bound, not a complete hierarchy;
  artifact SHA-256: `aec4ef691744ab7bca226107757599d3de72b7a31e7c6a4148ed8a81ea5f6513`.
- Built the 1976--1978 spouse-concept seam slice from 6 registered documents
  and 408 pages. Its 18 design-fixed baseline coordinates (630 expanded) are
  the same explicit lower-bound evidence; artifact SHA-256:
  `ca52a2feb16f55a2cd37265312d03cc0c3c11c54b4492f960d8d9d67203b1d11`.
- Built the 1979--1993 pre-ER slice from 29 registered documents and 3,349
  pages. Its 90 design-fixed baseline coordinates (3,150 expanded) remain a
  lower bound; artifact SHA-256:
  `a9c25ad33669df29c188622324391b078c10ff1fc6aa546d574348eeda807665`.

## Next

- Build and commit the remaining three era source/page and design-fixed
  baseline hierarchy evidence slices in order, without representing those
  lower bounds as complete era rows.
- Record the exact absence-domain dependency stop: `O_H`, `O_P`, `M_h`, and
  `P_h` counts remain unknown until the global catalog, positive-field joins,
  and complete near-match source annotation exist.
- Add mirrored validators, strict parsing, digest pins, mutation rejections,
  and tier synchronization; run Black, Ruff, and focused/full tests as
  proportionate to the resulting implementation.
